when:
  - event: push
    branch: main

steps:
  deploy:
    image: docker:cli
    commands:
      - cd /home/abdulwahid/app/nest_backend
      - git fetch origin
      - git reset --hard origin/main
      - docker compose up -d --build
